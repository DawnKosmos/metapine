version: 1
packages:
  - path: "gen" #ordner in dem Code abgespeichert wird
    name: "gen" #package name
    engine: "postgresql"
    schema: "./migration/" #schema werden in diesem Ordner gesucht
    queries: "./migration/queries/" # queries werden in diesem Ordner gesucht
    sql_package: "pgx/v5" #go sql_package
